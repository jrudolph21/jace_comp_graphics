## [Lab] - Code and test the vec3 class

In starterCode cmake file: 
 - create folder and add vec3.h
 - add {yourTest}.cpp program under set()
 - add include_directories for specific folder

Then in buildVCPkg run:
cmake ..
cmake --build .

Then do ./src/to/.exe

## [Lab] - Code and test the ray class

In the buildVCPkg: 
    cmake ..
    cmake --build .
    executable will be under /utests/Debug/utest_ray.exe